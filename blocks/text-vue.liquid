{% render 'text-vue', block: block %}

{% schema %}
{
  "name": "Text (Vue)",
  "tag": null,
  "settings": [
    {
      "type": "richtext",
      "id": "text",
      "label": "Text"
    },
  ],
  // presets 用于在 Shopify 主题编辑器中配置 block 的显示信息，决定它在“添加 block”列表中的名称和分类。
  "presets": [
    {
      "name": "Text (Vue)",
      "category": "Basic"
    }
  ]
}
{% endschema %}